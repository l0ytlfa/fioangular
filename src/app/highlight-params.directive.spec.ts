import { HighlightParamsDirective } from './highlight-params.directive';

describe('HighlightParamsDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightParamsDirective();
    expect(directive).toBeTruthy();
  });
});
